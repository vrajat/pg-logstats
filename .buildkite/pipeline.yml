steps:
  - label: ":rust: format"
    key: "format"
    agents:
      queue: "getafix"
    command:
      - ".buildkite/scripts/bootstrap.sh"
      - "make fmt-check"

  - label: ":rust: test"
    key: "test"
    depends_on: "format"
    agents:
      queue: "getafix"
    command:
      - ".buildkite/scripts/bootstrap.sh"
      - "make test"

  - label: ":rust: clippy"
    key: "clippy"
    depends_on: "format"
    agents:
      queue: "getafix"
    command:
      - ".buildkite/scripts/bootstrap.sh"
      - "make clippy"

  - label: ":package: package smoke"
    key: "package-smoke"
    depends_on: "format"
    agents:
      queue: "getafix"
    command:
      - ".buildkite/scripts/bootstrap.sh"
      - "make package-smoke"
